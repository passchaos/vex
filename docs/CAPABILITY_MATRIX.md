# Vex 能力矩阵

本文档把“实现完整的超越 Graphviz 能力”拆成可验证的交付项。它不是功能宣传页，而是完成度审计清单。

## 状态定义

- **已验证**：存在原生实现、公开入口和直接覆盖该能力的测试或命令证据。
- **部分完成**：已有实用实现，但仍缺少明确列出的语义、规模或质量门槛。
- **未实现**：尚无可依赖的产品能力。
- **明确排除**：项目决定不实现，不计入完成门槛。

只有所有非排除项都达到“已验证”，长期目标才可以标记完成。`zig build test`、文档列表或单个示例通过都不能单独证明目标完成。

## 产品边界

| 能力 | 状态 | 当前证据 | 完成门槛 |
| --- | --- | --- | --- |
| 原生 Zig parser/layout/SVG 管线 | 已验证 | `src/root.zig` 的 `parseInput`、`layoutGraph`、`render`；`zig build test` | 正常 CLI/API 路径不调用 Graphviz |
| SVG 输出 | 已验证 | `OutputFormat.svg`、CLI/API 示例 | 保持 CLI/API 和测试覆盖 |
| PNG/PDF/HTML/terminal 输出 | 明确排除 | 项目只注册 SVG 后端 | 不加入兼容负担 |
| Graphviz HTML-like label 渲染 | 明确排除 | angle-string 纯文本测试 | angle-string 始终按纯文本和 XML 安全方式输出 |
| C ABI、Python/其他语言 FFI | 明确排除 | 构建只安装 Vex CLI，并公开 Zig package API；无 header、外语 library 或 binding 目标 | 不维护跨语言 ABI 兼容负担 |
| Graphviz 作为 oracle | 已验证 | SVG oracle/residual 测试与 `tools/svg_residual.py` | 只用于开发测试，不成为运行时依赖 |

## Graphviz 生态兼容

| 能力 | 状态 | 当前证据 | 尚缺内容 / 完成门槛 |
| --- | --- | --- | --- |
| 普通 DOT label 字符实体与确定性字体度量 | 已验证 | Graphviz `entities.html` / `htmlEntityUTF8()` oracle；内嵌完整 252-name HTML 4 表，支持十进制/十六进制 numeric references；在 charset normalization 与 object escapes 后对 graph/node/subgraph/edge 的 `label`、`xlabel`、`headlabel`、`taillabel` 解码，unknown/invalid 保持原文，quoted node ID 身份不变，angle-string 与 typed API 不解码；`Symbol` face 使用内嵌 StandardSymbolsPS/Pango 14pt advances 与 Graphviz 2.42 integer span rounding，默认 ellipse 按 `poly_init` 的 padded-label minimum ellipse 精确拟合；所有节点均有该精确度量的无显式 `nodesep` 图再在 crossing/stress/extent/edge-length 单调约束下用 Graphviz 18pt rank gap；真实 `Symbol.gv` quality gate 为 0 overlap / 0 crossing，normalized edge/area `1.175/3.080` 对 Graphviz `1.170/2.743`，节点尺寸逐项匹配；≤128-node 图的 regular `Helvetica` ASCII ellipse 同样使用内嵌 DejaVu Sans/Pango 13pt/14pt quarter-point advances，真实 `2193.dot` 的58/58节点尺寸误差均改善、平均绝对误差 `42.22pt→0.37pt`，0 overlap/crossing、edge/area `1.859/5.819` 对 Graphviz `1.669/5.187`；all-node显式regular Times ASCII ellipse、≤64-node图复用DejaVu Serif 13pt/14pt表，真实`b94.gv` 10/10节点误差均改善、均值`47.32pt→0.002pt`且无 crossing/overlap回退；默认font、混合shape或更大Times图继续保留 crossing-validated conservative metrics（全局原型会使`overlap.gv` crossing `193→206`，已拒绝）；quality audit 的 Graphviz plain reader 对复杂 backslash label 自动回退 `-Tjson`；coordinate interval projection 对 epsilon-inverted zero-width interval统一折叠，不再触发 `clamp` panic | 保持 plain-label entity 与未来 HTML-like label renderer 的边界清晰；扩充 Times/Helvetica variants 等确定性 font metrics 后再扩大精确 shape fit 的节点规模与形状覆盖 |
| DOT 实用语法 | 部分完成 | strict、named/anonymous scoped subgraph、ports、port-preserving node-list fanout、whitespace-free edge operators、edge chains、rank groups、Graphviz NAME/NUMBER lexical boundaries、`chkNum` ambiguous-number token split、balanced angle-string、BOM、字符串、注释与多 top-level graph stream 测试；named subgraph textual ID 仅在 parser 内承担 parent-scoped reopen / compound 引用，未显式设置 `label` 时 model label 为空、SVG 不输出文字且布局不预留标题带；Graphviz 2835 stream 回归保留空 named subgraph 及 attrs 到 model/metadata，但 layout box 近零且不输出可视 cluster group；Graphviz `labels.c` AGRAPH object-escape 语义：subgraph label 中 `\G` 展开当前 textual ID、`\N` 保持字面量、`\L` 单次替换原始 label，并有 nested corpus / SVG 回归；Graphviz 七种 Latin-1 aliases 与 `big5` / quoted `big-5` 在 per-graph model boundary 原生转为 UTF-8，Big-5 使用内嵌 Graphviz lead/trail 范围映射、malformed bytes 转 U+FFFD；默认/显式 UTF-8 遇 invalid bytes 按 Graphviz 回退 Latin-1，XML 1.0 禁止控制码输出 U+FFFD，真实 `Latin1.gv` / 1472 XML-valid smoke；`parseDotGraphs` / `parseInputGraphs`、跨 graph 诊断、CLI 多 SVG 文档输出及 `src/testdata/dot_non_html_*.dot` corpus；Graphviz #2743 joined-attribute fixture 解析为 2 nodes / 1 edge；`zig build audit-dot-corpus -Dgraphviz-root=...` 用 ReleaseFast `--check` 固化本机完整 808-file DOT/GV baseline（上限16 MiB）：737 个非 HTML DOT 直接通过、65 个 HTML-like label 排除、5 个已知 malformed 正确拒绝、1 个 plain output 排除、0 timeout、0 unexpected；最大通过项 `2593.dot` 为 284,032 nodes / 142,476 edges | 继续扩展非 HTML-like label 的目标 DOT grammar corpus；所有支持/拒绝项有诊断测试 |
| 核心图模型与 Zig API | 已验证 | `Graph`、`NodeId`、typed attrs、API 示例与测试；node/subgraph parser textual ID 与 display `label` 分离 | 保持 `NodeId` 身份、`label` 显示语义和 parser 局部 textual-id 映射 |
| strict / keyed edge identity | 已验证 | Graphviz `agedge` source oracle；strict DOT/API duplicate-edge、non-strict same-key reopen、different-key parallel edge、undirected canonicalization 和 file corpus 测试 | strict 保持端点唯一；`key` 仅留在 parser 边界 |
| layered/Sugiyama 布局 | 部分完成 | rank、crossing、coordinate、long-edge、compound、nested cluster、`clusterrank=local|global|none`、typed graph `newrank` / `remincross` / `mclimit` / `nslimit1` / `nslimit` / `searchsize` / `TBbalance` / numeric-fill-compress-auto-expand ratio 与 typed subgraph `compact` strong-rank 测试；`zig build audit-cluster-corpus -Dgraphviz-root=...` 对 259 个小于 256 KiB 的 non-HTML cluster fixtures 做原生 layout+SVG metadata gate：257 valid、2 known malformed、0 timeout/failure/invalid SVG/invalid hierarchy，每个非空 child geometry 必须被 declared parent 包含；cluster `margin` 按 Graphviz `dotgen/position.c` 的 `late_int` 使用整数 point-space，覆盖 22pt、整数前缀、负值钳零、小数截断、非法值回退，并与 graph/node inch-space margin 回归隔离；cluster label band 按 Graphviz TOP/BOTTOM border 语义跟随显式或继承的 `labelloc`，bottom label 完整预留在成员下方，覆盖 nested containment 与 TB/LR；nested box 采用 deepest-first 传播 descendant label/margin/sibling bounds，覆盖三层 DOT 与四层 typed 空祖先 hierarchy；rank-simplex tight-tree 使用唯一 preorder subtree intervals，3×4 crossed parallel-rank 回归不再触发 `DisconnectedTightTree`；numeric/fill/expand 按 Graphviz `set_aspect` 拉伸 node centers、waypoints 和 subgraph bounds，size/page/aspect目标始终使用最终physical x/y轴而不随horizontal rankdir交换，node/text尺寸保持不变；需要扩大label box的默认serif parallelogram/octagon复用内嵌DejaVu Serif ASCII advances并按Graphviz `poly_init` 的PAD、椭圆、side-angle、skew bbox公式拟合，使LR `pgram` normalized edge/area `17.92/22.80→12.05/11.15`；`ratio=compress` 按 rankdir 使用 `size` 的 coordinate axis 目标压缩完整 virtual ranks，保留 same-rank 硬间距且不可行请求停在无重叠最小宽度；`ratio=auto` 按 Graphviz `idealsize(.5)` 使用 `page` 计算整数页网格，覆盖 TB/BT/LR/RL 和 typed `GraphAttr.page`，SVG 保持单完整文档而不分页；`TBbalance=min|max` 在所有 rankdir 中把无约束 source/sink floater 移到对应 boundary，显式 `rank=source|sink` 保持独占优先；`mclimit` 按 Graphviz MaxIter=24 / MinQuit=8 同时缩放最大 crossing passes 与连续无改进退出阈值，`.05` 对照 fixture 从默认 0 crossing 降低 effort 到残留 1 crossing，`vex_crossing_passes` 保留直接 override；`nslimit1` 按 `floor(value * node_count)` 限制 normal/strong rank network-simplex pivots，0/非法值禁用 simplex，quality fixture 的 weighted rank-span cost 从默认 167 变为 170，并覆盖 Graphviz 1902 / #2391 zero-budget smoke；`searchsize` 默认30，限制 ordinary rank simplex 每次 pivot 比较的负 cut-value tree-edge candidates，旋转 cursor 测试与修复后 quality fixture 覆盖显式 `1 => cost 604`、显式 `30 => cost 569`，未设置时后置exact closure进一步降至524；`nslimit` 按相同 node-count 规则限制 coordinate refinement passes，0/非法值跳过 refinement，quality fixture 默认 coordinate stress 至少比 zero-budget 低 2 倍，`vex_coordinate_passes` 保留直接 override；默认 integrated `newrank=true` 允许 rank constraints 横跨 sibling subgraphs，显式 false 恢复 Graphviz local recursive ranking 的 rank-constraint scope，rank constraint model/metadata 保留 named-subgraph scope，并覆盖 Graphviz 705 / #1221 / #2521 语义；local模式下Graphviz命名的`cluster*`按depth-first、同层first-sibling induction消解重叠成员，scoped rank与可视box仅使用有效成员而model/metadata保留声明集合，真实`1767.dot`在default/显式newrank true/false均保持chain层级，normalized edge/area `4.30/10.45→2.27/8.58`（Graphviz `2.13/6.38`）且crossing/overlap为0；active same-rank集合收缩后再在component图上做DFS反馈约束识别，仅从private rank objective屏蔽反馈edge而不反转公开端点，保持cluster back-edge routing契约，使真实`2825.dot`从`5 crossings / 11.39 edge / 21.41 area`降至`3 / 3.23 / 5.47`，优于Graphviz crossings/area `5 / 5.67`且edge接近其`2.82`；node `width`/`height`仅在72pt/in换算仍可表示为signed point-space时生效，超界/非有限值按Graphviz回退shape/label最小尺寸；无显式`nodesep`的edge-free single rank再按Graphviz默认18pt打包，使真实`2784.dot`从超大`2.16e18pt`画布、normalized area `9.93`降至有限`1.80`（Graphviz `1.30`），expanded 480-file另6项仅改善area；≤20节点、≤40边且多rank slot排列组合≤8192的小cluster图再做精确联合排列，保持既有slot与每个cluster同层成员连续性，以physical crossings、edge length词典序选择，使原始/annotation-heavy `clust5`声明顺序均为0 crossing，round-trip指标`2/2.74/5.16→0/2.61/5.03`，Graphviz为`0/2.50/5.88`，expanded 480-file同时使四份compound副本`1→0`且无overlap/edge回归；多行`doublecircle`按Graphviz先对padding label做enclosing ellipse fit，再加入双外围线4pt间距，修复过小pipeline节点，使原始`pmpipe.gv`从2 crossing降至0、edge/area为`2.43/3.85`对Graphviz `2.30/3.22`，expanded 480-file其它4个family无crossing/overlap回归；最终`ordering=out`投影按反声明顺序稳定合并多tail局部neighbor子序列，再仅允许保持所有ordering约束的等宽crossing下降交换，使`b58.gv`同时保持`1→2`与`7→5→4`、crossings `3→1`匹配Graphviz，edge/area `2.22/3.49`对`2.13/3.12`，expanded 480-file其余不变；显式`splines=ortho`的无向图在private layered view中保留文本端点方向，公开graph/SVG仍为无向，使真实电路`1658.dot`从`6 crossings / 2.50 edge / 5.56 area`变为`4 / 3.28 / 6.95`，crossing/area匹配Graphviz `4 / 6.95`且edge在13%内，expanded其它不变；local cluster中若所有有incident edge成员已在同一rank，则未被显式rank固定的孤立成员并入该active rank，避免泄漏到root rank0，使`grdcolors.gv` normalized area `14.72→3.55`接近Graphviz `3.44`且edge仍略优，expanded另2项改善、crossing/overlap不变；对zero-crossing、clusterless且`ordering=out`的DAG，将每rank压为rigid minimum-width sequence后只用edge least-squares求rank平移，候选须保持0 crossing并缩小extent，从而消除NodeId首声明敏感性，使round-trip `grammar.gv` edge/area `2.88/8.67→1.90/4.39`优于Graphviz `2.02/5.18`，原始grammar与awilliams面积也改善；最终physical-rank adjacent transpose保持异宽pair外边界，仅在真实rank-height crossing严格下降、coordinate stress不超过4%且extent不增时接受；expanded 480-file中21项改善且crossing/overlap零回退，固定fixture `NaN 82→51`、`b71 9→6`、`abstract 58→57`，并改善crazy/weight/jsort family；从相同输入独立保留equal-crossing/stress严格下降plateau候选，最终仅以更少physical crossings或同crossing更短physical edge覆盖strict-only路径；后续480-file diff中48项改善且crossing/overlap/edge/area零回退，original `NaN 51→48`、`viewfile 8→5`、`2413_2 64→60`，LR rankdir round-trip `5→4`匹配Graphviz；32–64节点、baseline 1–8 crossing的clusterless图再保留独立6-restart physical adjacent barrier候选，内部可越过crossing势垒但每个保留状态须满足4% stress/extent预算且最终只按完整physical objective覆盖strict/plateau路径；Graphviz round-trip `width/height`仅使unix2两个节点约增宽2.2pt/0.2pt却联动4个rank order，该候选使original及share/windows/dotsplines variants均稳定为`2 vs 3`，expanded holdout 15项改善且crossing/overlap/edge/area零回退；≤31节点directed clusterless DAG中的equal-weight one-in/one-out pass-through在全可行区间weighted rank cost等价时选择midpoint而非受单节点occupancy tie固定到边界，使真实port-constrained `2734.dot`的node22从rank9移至8、crossing `1→0`匹配Graphviz，expanded其余479项不变；私有定向的undirected graph不启用，保留1658 circuit指标；默认local ranking下，对2–8个互不重叠/无嵌套的top-level clusters、≤32节点/64边、每节点唯一归属且每cluster至少一条internal DAG edge、无rank/newrank override的图，先求cluster local offsets，再以Graphviz `interclust1` slack node与`CL_BACK=10` tail anchoring求cluster base并expand offsets；真实`2521_1.dot`从9个singleton ranks/1 crossing收缩为5层timeline/0 crossing，edge/area `2.12/4.09`对Graphviz `2.00/4.13`，expanded其余479项不变；纯视觉cluster与internal cycle明确拒绝；`compact=true` 使用 Graphviz newrank 的 1000 权重 rank-span 目标、weak cross-edge penalty、nested strong envelope，并覆盖 internal `minlen`、重叠 rank set 与显式 boundary rank；`remincross` absent/true 在 cluster grouping 后运行第二次全局 crossing minimization，false 保留稳定 first-pass block order，非法显式文本按 Graphviz mapbool 关闭；global/none 保留 rank constraints 并禁用特殊 subgraph ranking/box/spacing/compound-boundary routing；`audit-layout-quality` 在真实 sparse-undirected `ngk10_4.gv`、dense-undirected `overlap.gv`、directed-DAG `fig6.gv`、explicit-rank `world.gv`及annotation-heavy `share/world.gv` round-trip、partial explicit-rank `shells.gv`、weighted cyclic profile `698066.dot`、wide zero-crossing star `1855.dot`、overlapping-cluster rank `1767.dot`、nested-owner component `2476.dot`、contracted-rank-cycle `2825.dot`、overflowing-node-dimension `2784.dot`、port-constrained small DAG `2734.dot`、disjoint local-timeline cluster `2521_1.dot`、cluster round-trip `share/clust5.gv`、multiline-doublecircle pipeline `pmpipe.gv`、overlapping `ordering=out` `b58.gv`、orthogonal circuit `1658.dot`、sparse cluster palette `grdcolors.gv`、grammar round-trip `share/grammar.gv`、ordered-DAG `awilliams.gv`、cross-cluster cycle `try.gv`、cyclic-workflow `b57.gv`、acyclic fanout `abstract.gv`、heterogeneous workflow `b71.gv`、fragmented DAG `mike.gv`、self-loop/cyclic dependency `NaN.gv`、feedback-heavy workflow `rowe.gv`、cubic undirected `Heawood.gv`、private-branch DAG `unix2.gv`及size-annotated `share/unix2.gv` round-trip、annotation-heavy LR `linux.x86/rankdir_dot.gv` round-trip、horizontal fill-aspect `pgram.gv`、reciprocal cluster/rank `dfa.gv`、flat-cluster `proc3d.gv` 与 deeply nested compound-cluster `1332.dot` 上以 invariant geometry 比较 Vex/Graphviz：均为0 overlaps，crossings 分别 `182 vs 206` / `193 vs 283` / `58 vs 62` / `59 vs 66` / `59 vs 66` / `8 vs 8` / `17 vs 24` / `0 vs 0` / `0 vs 0` / `0 vs 1` / `3 vs 5` / `0 vs 0` / `0 vs 0` / `0 vs 0` / `0 vs 0` / `0 vs 0` / `1 vs 1` / `4 vs 4` / `0 vs 0` / `0 vs 0` / `0 vs 0` / `0 vs 0` / `0 vs 0` / `57 vs 58` / `6 vs 11` / `6 vs 7` / `48 vs 96` / `52 vs 63` / `1 vs 10` / `2 vs 3` / `2 vs 3` / `4 vs 4` / `0 vs 0` / `0 vs 0` / `0 vs 0` / `8 vs 9`、aggregate `770 vs 990`；三项 normalized edge length/area 分别为 `6.485/10.657 vs 11.962/26.041`、`5.492/11.310 vs 6.082/11.109`、`5.455/7.928 vs 4.925/7.256`，`dfa` edge length `1.599 vs 1.620`；density-aware BFS DAG orientation：10–16节点3-regular undirected图保留声明cycle方向并只反向feedback edge，使Heawood 17→1、Petersen 8→2 crossings且edge/area仍优于Graphviz；dense component保留首声明root并优先高degree邻居，sparse component使用declaration-order/NodeId 相比 `ngk10_4` 旧版653 crossings 显著改善、对32–128-node leafless sparse component选halfway-shell declaration median root、对私有directed view执行equal-cost balance且以密度门槛隔离 dense mesh，directed equal-cost rank balance 将 `fig6` max rank width 12降至8；40+节点无cluster DAG中branching predecessor→simple-shape private successor的等权long-slack chain保留tight incoming rank，不被occupancy balance推入中层，使unix2 crossings 9→2且edge/area显著收缩；final center transpose 继续降低真实几何交叉且同样安全用于 cluster graph，reciprocal edge pair 统一按首次声明方向参与 rank objective 且不改原图/SVG箭头端点，directed cycle在私有layout view中反向DFS back edge且不改SVG箭头语义，32–128节点且E/V≤2.2的medium sparse workflow镜像cgraph insertion-head邻接遍历、dense mesh与tiny FSM保持正声明序，使rowe 73→52 crossings，self-loop不参与Kahn indegree/minlen或feedback分类；无cluster DAG及至少48节点的private cyclic DAG view复用maximum-weight closure精确下降weighted rank span，但候选若增大peak rank occupancy则回退；中型cyclic图最终把每rank压成rigid minimum-width sequence并对rank平移量做edge least-squares coordinate descent，候选仅在physical crossings、stress、extent均不劣时接受并同步virtual waypoints，使NaN在rigid-rank阶段达到82 vs 96 crossings且edge/area均优于Graphviz；ordering=out按distinct neighbor去重parallel edges，最终用edge-declaration DFS preorder投影合法rank slots并在zero-crossing预算内压缩coordinate stress；最终rank阶段统一迭代same/boundary constraints与active hard edges直到联合可行，修复world的6->7同rank minlen违规；全节点显式same-rank图在stress compaction后做确定性all-slot insertion sifting，以真实physical crossings为主目标、4% stress为约束，使world 65→64且Graphviz annotation round-trip变体89→85并降低edge stress；随后保留声明序候选并增加仅按`(from NodeId,to NodeId)` SplitMix64优先级初始化long-edge dummy的确定性restart，以精确virtual crossings择优，使原版与annotation-heavy round-trip均稳定为`59 vs 66`且normalized area降至7.75；部分覆盖图只在该level每个节点均属于active same-rank constraint时参与compaction/sifting，异宽重排保留原level左边界与逐slot gap，未覆盖level完全不动且compaction不允许crossings上升，使`share/shells.gv` normalized edge length `3.70→3.59`；对≤40节点、部分same-rank覆盖且covered-level permutation总数≤4096的图，再固定未覆盖level与每个covered level最左anchor，穷举联合层序并对每个顺序做nodesep约束的convex coordinate descent，仅接受physical crossings严格下降、center-edge length/extent/clearance均不退化的候选，使shells `13→8`匹配Graphviz且edge/area降至`3.09/5.91`；显式 same-rank constraint 在私有 layout view 中把对应edge有效minlen归零，使 `proc3d` normalized area `8.041 vs 8.459` 且crossings从旧版13降至0；medium cluster graph 用 maximum-weight tight closure 补足单 seed/simplex 的局部 rank optimum，使 `1332` weighted rank span `253 -> 133`；nested spacing 以 deepest subgraph owner 区分同rank节点，并在已超出普通绝对compactness预算时复用一次固定增量allowance，使crossings `126 -> 17`，post-spacing真实中心transpose仅接受crossing严格下降、coordinate stress不超过4%且同层clearance仍满足的槽位交换；small directed graph再用固定种子13-restart搜索跨越pair-swap局部势垒，并从同一输入并行保留arbitrary-slot→continuous与width-aware adjacent→continuous两条候选，后者保持异宽节点pair外边界不变且最终只接受真实physical crossing更优解，使b71从26降至14并同时改善jsort/unix/crazy family；对48–64节点且baseline crossings≥10的medium sparse candidate把固定restart预算从13增至64，使b71进一步`14→13`、expanded holdout `698066.dot` `29→27`，其余480-file样本不变；48–64节点无cluster图中equal-weight heavy degree-2 pass-through的全可行rank区间对weighted span等价，改选source-side quartile以保留sink-side long-feedback channel，使`698066.dot`从`27→17 vs 24`，normalized edge/area `2.48/5.88`优于Graphviz `3.92/6.68`，expanded 480-file其余479项不变；无显式nodesep、两层、宽层≥8节点/另一层singleton且baseline零crossing的star候选按Graphviz默认18pt nodesep重打包，仅在crossings仍零且edge length/extent严格改善时采用，使1855 normalized edge/area `30.87/75.49→24.78/49.00`，接近Graphviz `24.81/48.00`；medium moderate-crossing width-aware搜索直接补入两个稀疏late SplitMix checkpoint，并在候选上固定同层顺序、按incident weighted optimum做physical-crossing不增/coordinate-stress严格下降/extent不增的plateau收敛，使b71 `13→9 vs 11`、jsort `68→66 vs 107`，expanded 480-file其余变化均仅改善edge/area；clusterless exact closure仅在24–39节点窗口允许增大peak rank occupancy，以短branch收紧收益换取一个更宽中层，使mike `10→6 vs 7`且edge length显著下降，窗口外继续保留occupancy guard；无cluster图最后在固定层内顺序下尝试clearance interval端点/中点/邻居中心，连续坐标sifting只接受crossing严格下降、stress不超过4%且extent不增加的位移，使ngk10_4/overlap/fig6 crossings均低于Graphviz；nested cluster最后以deepest owner为移动单元尝试其他owner bounds前后/中心对齐，整组位移在extent不增、同rank clearance与4% stress约束下使1332 crossings进一步 `17 -> 11`；48–64节点directed clusterless feedback图在所有独立center候选选定后再运行一次stress下降plateau，只有physical crossings严格下降、edge length/extent不增且stress在4%内才采用；真实`b57.gv`从`1→0` crossing匹配Graphviz，edge/area从`3.878/8.092→3.803/7.636`，clean 480-file diff仅该项变化，原始undirected及显式rank/group/ordering均排除；两个cross-linked TB/BT cluster的最终Graphviz-style visual-padding微调现在以最终NodeLayout真实中心线crossing作原子回滚guard，真实`try.gv`及share/windows副本均从`1→0`匹配Graphviz、edge/area保持`3.062/3.872`，clean 480-file diff仅这三份变化；nested cluster owner最终按同owner内部弱连通分支拆分移动单元，可对齐同owner或descendant-owner边界，但仅在physical crossings严格下降、edge length/extent不增、stress在4%内且clearance/ordering合法时采用；真实`2476.dot`从`2→0` crossing优于Graphviz 1，edge `4.322→4.053`优于Graphviz `4.923`，clean 480-file diff其余479项不变；随后用真实rank height/rank gap物理深度计分，先做strict crossing descent、再允许equal-crossing stress下降plateau，最后尝试非ancestor/descendant bounds对齐的ancestor subtree strict move，使1332达到 `8 vs 9` | 继续完整目标 cluster 语义 corpus；继续扩展 force 等多类质量 fixture 与更广 aggregate 门槛 |
| force-directed 布局 | 已验证 | independent neato stress-majorization；independent fdp spring-electrical with K/T0/len/weight and cluster boxes；independent sfdp deterministic coarsen/prolongate/refine with levels/K/repulsiveforce；deterministic Fruchterman-Reingold；Graphviz `normalize` final-coordinate rotation；规模、能量、属性、引擎区分和预算测试；`audit-layout-quality` 显式用 neato 比较真实 `Petersen.gv` invariant geometry，Vex/Graphviz 分别为 overlaps `0/0`、crossings `9/11`、edge-length CV `0.540/0.588`、normalized stress `0.381/0.449`，stress majorization 后复用确定性 minimum-axis overlap removal；同一 audit 用 sfdp 比较 `Petersen.gv` 得到 `0/0` overlaps、`3/3` crossings、`0.242/0.206` CV、`0.171/0.174` stress，默认 sfdp 质量预算240而显式预算保持原值；用 fdp 比较无 `len/weight/pos` 干扰的 `p2.gv` 得到 `0/0` overlaps、`0/1` crossings、`0.250/0.338` CV、`0.040/0.102` stress；fdp/sfdp 非增量路径同样做确定性 overlap removal | 保持四条引擎路径独立，持续扩展规模基准与更复杂 force oracle fixture |
| radial/twopi 布局 | 已验证 | explicit/auto root、per-component node `root=true`、BFS rings、Graphviz scalar/list `ranksep` radial increments、subtree angular spans、component packing、CLI/Zig API 和 SVG smoke | 保持 root `NodeId` 与 textual id/display label 分离 |
| circular/circo 布局 | 已验证 | Tarjan biconnected blocks、block-cut circles、graph `root`、per-component node `root=true`、mindist、oneblock、component packing、CLI/Zig API 和 SVG smoke | 持续补充复杂 articulation oracle corpus |
| treemap/patchwork 布局 | 已验证 | hierarchical squarified treemap、typed node/subgraph area、nested containment、area override、edge independence、CLI/Zig API 和 SVG smoke | 持续补充大层级与 label-fit corpus |
| array/osage 布局 | 已验证 | recursive child-subgraph-first packing、intrinsic node/cluster rectangles、typed `pack`/`packmode`/`sortv`、row/column-major 与 alignment flags、nested containment、edge independence、CLI/Zig API 和 SVG smoke | 持续补充复杂异形节点与 Graphviz residual corpus |
| pre-positioned nop/nop2 布局 | 已验证 | required node `pos`、typed `NodePosition`、`!` pin parsing、`notranslate`、nop edge reroute、nop2 `3n+1` cubic edge spline preservation/fallback、typed `EdgeSplineSegmentInput`、input cluster `bb`、graph/subgraph/node/edge `lp`/`xlp`/`head_lp`/`tail_lp`、deterministic `overlap` families 与 typed `sep`、nop2 final-geometry isolation、CLI/Zig API 和 SVG smoke | 持续补充 Graphviz residual corpus |
| rankdir、rank constraints、spacing | 已验证 | TB/BT/LR/RL、same/min/max/source/sink、ranksep/nodesep、DOT/API `minlen=0` same-rank edge 与 Graphviz #162 inter-subgraph 单边测试；Graphviz mapbool 语义和 #258 `constraint=none` 单边测试；`weight=0` 保留 hard minlen、退出 rank/coordinate objective 但保留 crossing xpenalty 的 rank/metadata 测试 | 所有方向持续通过 |
| records、ports、compound edges | 已验证 | record field、compass port、scoped ltail/lhead 与 clipping 测试；Graphviz `compound.c` endpoint-membership oracle，目标 subgraph/后代必须包含对应 tail/head 且不能同时包含两端，无效 hint 保留 attr/metadata 但回退普通节点边界；DOT parser 保留 textual raw attr，typed API 只存结构化 `SubgraphId` 且不把 display label 伪装成 raw attr，两者 SVG metadata 均输出 numeric endpoint ID；DOT corpus 与 typed ancestor-membership 回归 | DOT/API 与 metadata 语义一致 |
| parent-scoped subgraph identity | 已验证 | Graphviz `agsubg` source oracle；same-parent reopen merge、different-parent isolation 和 file corpus 测试 | 保持成员/属性合并和最近作用域引用语义 |
| Graphviz 常用 shapes/styles/colors/fonts/images | 部分完成 | Graphviz shape 注册表差分仅排除 PostScript-only `epsf`，其余 canonical shapes/aliases、SBOLv、最多四段的复合 arrow marker grammar（open、gap、`l`/`r` half modifiers、typed `ArrowPart`）、完整 265 个 ColorBrewer palette variants / 1,689 indexed colors、gradients、layers、fonts、`image`/`shapefile`/`imagescale`/`imagepos` 与 graph `imagepath` first-readable search 均有 typed/API/SVG 测试；graph `quantum` 按 inch→point quantum 向上量化 estimated label dimensions，并覆盖 layered/force 与非法值 no-op；仅服务 image-map outline 的 `samplepoints` 不暴露 typed SVG API | 继续用 oracle corpus 收敛非 HTML 渲染几何差异 |
| SVG canvas 与对象分组 | 已验证 | size、numeric/fill/compress/auto/expand ratio、dpi、rotate、center、outputorder、id、class 测试；layered ratio modes 均先处理 layout coordinates，SVG 只应用最终 physical size / page-grid target 而不二次调整 viewBox aspect；超大 `minlen=60000` 虚拟 rank/waypoint、256-node/768-edge crossing-grid 与 5,002-node/7,330-edge/1,002-rank tall-narrow ReleaseFast gate 覆盖线性坐标计算、虚拟层/real-level Fenwick crossing 计数、rank-interval edge index、generation-stamped sparse virtual-position map、SVG node-rect spatial index 及完整输出；真实 Graphviz `2854.dot` 默认布局+SVG 从约 `9.49s` 降到 `0.66s` 且 SHA-256/bytes 与优化前一致；`audit-svg-corpus` baseline：727 non-HTML candidates = 722 fast XML-valid + 0 slow + 5 known exclusions；`audit-large-svg-corpus` baseline：16 large non-HTML candidates = 15 valid + 1 malformed + 0 slow，均为0 timeout/failure/nonfinite/invalid XML | XML 安全与 Graphviz-style group 结构持续通过 |

## 超越 Graphviz 的能力

| 能力 | 状态 | 当前证据 | 尚缺内容 / 完成门槛 |
| --- | --- | --- | --- |
| 自包含交互 SVG | 已验证 | layer、collapse、filter、labels、focus、inspector、search、viewport、minimap、stats 和 `interactive-all` 测试 | 键盘/ARIA 与静态模式回归持续通过 |
| 机器可读 SVG 对象索引 | 已验证 | graph/node/edge/subgraph 结构、attrs、rank constraints、ports、waypoints、geometry；`SVG_METADATA_V1.md` namespace/version/features/additive policy；静态模式与 CLI/API smoke | v1 只做 additive 扩展，破坏性变化发布新 namespace major |
| 精确解析诊断 | 已验证 | line/column/source/caret/hint；`parseDotDiagnostics` statement recovery、max cap、UTF-8、lexical stop；CLI `--validate-all` smoke | 持续随目标 grammar 增加语义诊断 |
| 可配置布局/输入预算 | 部分完成 | parse-only、max input、iterative engine budgets、layered pass budgets；DOT textual ID 通过 `NodeId` 索引 O(1) 反查；`zig build test-parse-scale` 使用 ReleaseFast 运行 10,000-node / 9,999-edge chain 与 4,096-node / 4,095-edge / 64-subgraph dense-attribute 两类 gate，每类限制 1 秒和 32 MiB parser arena，并报告 source/arena bytes 与 elapsed time；连续 cached 实测分别为 `5–6ms / 20.3MiB` 与 `6–8ms / 14.3MiB`；`zig build test-layout-render-scale` 覆盖 layered/neato/fdp/fr/sfdp/twopi/circo/patchwork/osage 全部原生布局族，使用 256-node / 512-edge iterative、192-node / 388-edge crossed layered、2,048-node / 4,096-edge sfdp/radial/circular 和 4,096-node packing workloads，保持 layout 3 秒 / 8 MiB arena、SVG render 1 秒 / 4 MiB arena 原预算，验证对象分组和输出 hash，并通过归一化 `getrusage` 限制进程 peak RSS 为 96 MiB；连续实测最大 layout arena `4.94MiB`、最大 render arena `1.42MiB`、组合 peak RSS `12.40–12.42MiB`，所有输出 hash 稳定且 layered deterministic double-render 保持 byte-identical；`zig build test-layered-tall-scale` 另以 5,002 nodes / 7,330 edges / 1,002 narrow ranks / long edges 固化默认 24-pass crossing 在 3 秒、40 MiB layout arena 内，当前实测约 `0.44s / 26.0MiB`，避免每层全图清空退化；`zig build test-waypoint-scale` 以 18,432 nodes / 16,896 edges / 152,064 waypoints 固化 layout/render 各 2 秒与 48 MiB arena，rank→nodes CSR 使真实 Graphviz `2646.dot` 从约 `7.90s` 降至 `4.54s` 且 51 MiB SVG SHA-256/bytes 不变；adjacent exchange 只重算被交换节点关联 edge-pair，使真实 `2064.dot` 从约 `7.01s` 降至 `5.58s` 且 31 MiB SVG SHA-256/bytes 不变；cluster membership O(1) 索引、无标签 edge bounds 短路、无 long-edge refinement 短路与 CLI `init.gpa` 使真实 `2108.dot` 连续约 `7.23s`，19 MiB SVG SHA-256/bytes 不变；fragmented-component layered packing 与 parallel-edge endpoint hash 使 284,032-node / 142,476-edge / 141,556-component 的真实 `2593.dot` 完整 SVG 流程约 `0.89s`；所有当前引擎 `LayoutControl` / `LayoutWorkBudget` 取消和 CLI exit-2 smoke；sfdp Barnes-Hut 精度/交互次数门槛和 512-node SVG smoke | 增加长时间趋势和并行吞吐门槛 |
| 增量布局与心理地图稳定性 | 已验证 | `layoutGraphIncremental`；layered/force 共享节点位移门槛、无重叠/边界和 `stability=0` 等价测试 | 保持 NodeId 驱动；共享节点位移显著低于完整重排 |
| 流式/并行大图管线 | 部分完成 | `visitDotGraphs` / `visitInputGraphs` 逐图 borrowed callback API 与 diagnostic variants：每个完成 graph 在 callback 返回后立即释放，顺序/peak-live=1/later-error consumed-count 回归；CLI `--check` 走该低驻留路径，真实 3-graph summary 及第二图错误前保留第一图 summary；CLI `--layout-workers=1` 使用逐图 parse→layout→SVG→flush pipeline，serial visitor 与 parallel owned output byte-identical，later parse error 保留已完成 SVG；`zig build test-parse-scale` 的 stream gate 以 8 MiB active-allocation limit 消费 64×256-node graphs，连续实测 `18–23ms`；`layoutGraphsParallel` 固定 worker batch API：输入保序、逐项 `.layout` / `.failure`、每任务独立 allocator/config、原子 batch cancel 与已有 `LayoutControl` 组合、单线程 target 串行 fallback；CLI 多 top-level graph stream 自动并行 layout，`--layout-workers` 可限流/强制串行，随后按输入顺序串行渲染 SVG，worker=1/4 byte-identical smoke hash `c96cfb98...`，每图独立 `--layout-work-budget` 保持 exit-2；`zig build test-parallel-layout-scale` 使用 4 workers 并行 8 个 256-node / 512-edge sfdp 图，逐任务 2 MiB arena、2 秒和 96 MiB peak-RSS 门槛，连续 hash `5ce71c860fab2181` / arena `650,256` bytes，实测 parallel `18–46ms` vs serial `70–84ms`、peak RSS `10.21–10.39MiB` | 输入字节仍一次性读取，单图 parser/layout 仍是内存内流程；继续实现真正增量字节输入、图内并行阶段和长期吞吐趋势 |
| 清晰模块边界 | 部分完成 | 已拆出 `src/layout/*`、`src/svg/*` | `src/root.zig` 仍承载 parser/model/layout/render 大量实现；继续按稳定边界拆分 |

## 验证门槛

每个功能增量至少需要：

1. 聚焦测试直接覆盖新增语义，而不是只检查命令退出码。
2. `zig fmt --check` 和 `git diff --check`。
3. `zig build test`。
4. 涉及 CLI/SVG 交付时，运行真实 CLI smoke。
5. 涉及 Graphviz parity 时，使用本机 Graphviz oracle 或 residual 证据；不得在产品运行时调用 `dot`。
6. 更新本矩阵中的状态、缺口或证据。

## 当前优先级

1. 目标 DOT grammar/corpus 与完整 cluster 语义。
2. 大图性能、内存和取消门槛。
3. 继续收敛 parser/model/layout/SVG 模块边界。
